import 'package:flutter/material.dart';

class OngoingBookingItem extends StatelessWidget {
  const OngoingBookingItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}